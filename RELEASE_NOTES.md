### Language setting now applies to the main menu

- 🌐 Changing the language in **Settings → Appearance** now takes effect immediately in the popover (the menu that appears when you click the menu-bar icon) — no restart required.
- Previously, language changes only updated the Settings window; the main popover (Usage, Costs, Accounts tabs) stayed in English.

#### Included from v1.8.2

- 📊 **Session (5h) & weekly (7d) usage bars** are now available as menu-bar modules, and are **on by default** for new installs (account name + both bars).
- ⏱️ **"Usage vs time" variant** — an optional version of each bar that overlays a thin vertical "time elapsed" marker. If the fill is past the line you're burning faster than the clock; behind it, you have headroom. A short caption in Settings explains it.
- 🧰 Pick whichever you like in **Settings → Menu Bar**: plain usage bar, or the usage-vs-time one.
- 📝 Release notes now show up in the in-app (Sparkle) update dialog and on the GitHub release page.

#### Included from v1.8.1

- 🗂️ Dedicated **"Menu Bar"** tab in Settings (the General tab no longer needs scrolling). "Show full email address" stays in General.

#### Included from v1.8.0

- 🧩 Configurable iStats-style menu bar: account name, 5-hour session usage, weekly usage, today's cost, and session/weekly reset countdowns — drag to reorder & toggle, with a live preview. Existing "show account name" preference migrated automatically.

---

### 语言设置现在对主菜单生效

- 🌐 在 **设置 → 外观** 中修改语言后，弹出菜单（点击菜单栏图标后出现的界面）**立即生效**，无需重启。
- 此前，语言切换只作用于设置窗口，主菜单（用量、费用、账户三个标签页）始终显示英文。

#### 包含 v1.8.2 的更新

- 📊 **5 小时会话用量条 / 7 天每周用量条** 现已加入菜单栏模块，新安装**默认开启**（账户名 + 两个用量条）。
- ⏱️ **「用量 vs 时间」变体** —— 每个用量条的可选版本，叠加一条细竖线表示「时间进度」。填充超过这条线 = 你用得比时间快；落在线后 = 还有富余。设置里有一句说明。
- 🧰 在 **设置 → 菜单栏** 里任选：纯用量条，或带时间对照的版本。
- 📝 更新说明现在会显示在 App 内（Sparkle）更新弹窗以及 GitHub 发布页。

#### 包含 v1.8.1 的更新

- 🗂️ 设置新增独立的 **「菜单栏」** 标签页（「通用」不再需要滚动）。「显示完整邮箱地址」保留在「通用」。

#### 包含 v1.8.0 的更新

- 🧩 可配置的 iStats 风格菜单栏：账户名、5 小时会话用量、每周用量、今日花费、会话/每周重置倒计时 —— 拖拽排序、勾选开关，带实时预览。自动迁移原有的「显示账户名」偏好。

**Full Changelog**: https://github.com/XueshiQiao/CCSwitcher/compare/v1.8.2...v1.8.3
